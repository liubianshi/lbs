% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lake.R
\name{register_lake}
\alias{register_lake}
\title{Register an R data frame as a raw entity in the econ-data data lake.}
\usage{
register_lake(
  df,
  id,
  columns = NULL,
  name = NULL,
  domain = NULL,
  author = NULL,
  status = c("draft", "stable", "deprecated"),
  source_url = NULL,
  license = NULL,
  notes = NULL,
  overwrite = FALSE,
  dry_run = FALSE,
  lake_path = NULL,
  econdata_bin = NULL
)
}
\arguments{
\item{df}{A \code{data.frame} / \code{data.table} to publish. Columns must be of
types Arrow / Parquet accepts (\code{string}, \code{integer}, \code{numeric},
\code{boolean}, \code{POSIXct}, \code{Date}). Binary / list columns will be rejected
downstream by the CLI's parquet schema check — clean them up before
calling.}

\item{id}{Target entity id, \verb{raw:<source>/<dataset>@<version>}. Currently
only \verb{raw:} is supported (dim / indicator / concord come via the
dedicated CLI flows).}

\item{columns}{Optional named list / vector of column descriptions. Two
accepted shapes per entry:
\itemize{
\item \code{firm_id = "企业唯一标识"} — plain description.
\item \code{revenue = list(description = "营业收入", dtype = "numeric")} —
description plus dtype override (when auto-inference would map to the
wrong vocabulary).
Columns not listed get \code{description: TODO} (so users see what's missing
when they \verb{entity show} the result).
}}

\item{name}{Human-readable display name. Defaults to the dataset segment of \code{id}.}

\item{domain}{Subject area. Defaults to the source segment of \code{id}.}

\item{author}{meta.yaml author field. Defaults to \verb{$USER} or \code{"TODO"}.}

\item{status}{\code{"draft"} (default) or \code{"stable"}. Use \code{"draft"} when the
column descriptions still contain \code{TODO}s.}

\item{source_url, license, notes}{Optional metadata pass-through to the
meta.yaml.}

\item{overwrite}{If \code{TRUE}, replace an existing entity with the same id.}

\item{dry_run}{If \code{TRUE}, print the planned meta.yaml + CLI invocation and
return without touching disk.}

\item{lake_path}{Override the lake root. Defaults to
\verb{$ECON_DATA_LAKE_PATH} or \verb{~/Data/econ-data-lake}.}

\item{econdata_bin}{Path to the \code{econ-data} binary. Defaults to
\code{Sys.which("econ-data")}.}
}
\value{
Invisibly, a \code{list(id, data_path, meta_path, cli_output, dry_run)}.
}
\description{
One-shot writer: takes a \code{data.frame} / \code{data.table}, infers a draft
meta.yaml from column classes, copies the data into the lake's canonical
path, then shells out to \verb{econ-data entity register --meta} so the same
validator (binary dtype rejection, RFC3339 timestamp enforcement,
cross-field id checks) runs as for direct CLI use.
}
\details{
If validation fails the placed \code{data.parquet} is rolled back so the lake
never ends up in a half-written state.
}
\seealso{
\code{\link[=read_archive_lake]{read_archive_lake()}} for the symmetric reader; the
\verb{econ-data entity scaffold} CLI command for the equivalent
shell-only flow.
}
