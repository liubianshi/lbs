% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logger.R
\name{logger_factory}
\alias{logger_factory}
\title{Create a Logger Factory}
\usage{
logger_factory(prefix, debug = NULL)
}
\arguments{
\item{prefix}{A single character string prepended (underlined) to every
log message.}

\item{debug}{\code{TRUE}/\code{FALSE} to force debug output on or off for this
logger instance, overriding the global \code{logger.debug} option.  When
\code{NULL} (default) the instance defers to \code{getOption("logger.debug", FALSE)} at call time, so toggling the option mid-session takes effect
immediately.}
}
\value{
An S3 object of class \code{logger} — a list of four closures:
\itemize{
\item \code{info()}  — informational \code{message()} in blue.
\item \code{warning()}  — \code{warning()} in yellow. Passes \code{call. = FALSE}
unless the caller overrides it.
\item \code{error()} — \code{stop()} in red. Passes \code{call. = FALSE} unless
the caller overrides it.
\item \code{debug()} — grey \code{message()}, emitted only when debug is
enabled (see \code{debug} parameter and \code{logger.debug} option).
}
}
\description{
Returns an object with four logging methods (\code{info}, \code{warning}, \code{error},
\code{debug}) that prepend a prefix and ANSI colour codes to each message.
}
\examples{
logger <- logger_factory("MyApp")
logger$info("Application started")
try(logger$warning("Low memory"))

# Enable debug logging via option (affects all loggers with debug = NULL).
old <- options(logger.debug = TRUE)
logger$debug("Variable x = 5")
options(old)

# Instance-level override — always debug, regardless of global option.
verbose_logger <- logger_factory("MyApp", debug = TRUE)
verbose_logger$debug("always shown")

# Instance-level override — never debug, regardless of global option.
silent_logger <- logger_factory("MyApp", debug = FALSE)
silent_logger$debug("never shown")

}
