% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lake-read.R
\name{lake_read}
\alias{lake_read}
\title{Read a registered entity from the econ-data data lake.}
\usage{
lake_read(
  database = NULL,
  table = NULL,
  id = NULL,
  var = NULL,
  condition = NULL,
  and = TRUE,
  limit = NULL,
  noinfo = TRUE,
  lake_path = NULL
)
}
\arguments{
\item{database}{lbs database name (e.g. \code{"CHN_FirmTrade"}). Ignored when
\code{id} is supplied.}

\item{table}{lbs table name (e.g. \code{"HG2005"}). Ignored when \code{id} is supplied.}

\item{id}{Optional explicit entity id, \verb{<type>:<domain>/<dataset>@<version>}
(e.g. \code{"raw:firmtrade/hg@2005"}). Takes precedence over \code{database}/\code{table}
and works for any entity type, not just \verb{raw:}.}

\item{var}{optional character vector of column names to project.}

\item{condition}{optional SQL \code{WHERE} clause(s); same semantics as
\code{\link[=read_archive]{read_archive()}}.}

\item{and}{\code{TRUE} (default) joins \code{condition} with \code{AND}; \code{FALSE} with \code{OR}.}

\item{limit}{optional integer row cap.}

\item{noinfo}{if \code{TRUE} (default) return the \code{data.table} directly; if
\code{FALSE} return \code{list(data, info)} matching \code{\link[=read_archive]{read_archive()}}.}

\item{lake_path}{data lake root. Defaults to the env var
\code{ECON_DATA_LAKE_PATH}, falling back to \verb{~/Data/econ-data-lake}.}
}
\value{
\code{data.table} (or \code{list(data, info)} if \code{noinfo = FALSE}).
}
\description{
Reads an entity's \code{data.parquet} via DuckDB and attaches column labels from
its \code{meta.yaml} (using \code{\link[=stlabel]{stlabel()}}), matching the attribute shape of
\code{\link[=read_archive]{read_archive()}}. The entity can be addressed either by an explicit lake
\code{id} or — for back-compatibility with the lbs DBMS archive — by a legacy
\verb{(database, table)} pair, which is translated through \code{\link[=lake_id]{lake_id()}}.
}
\seealso{
\code{\link[=read_archive]{read_archive()}} for the legacy file-based reader; \code{\link[=lake_meta]{lake_meta()}}
for the entity's full metadata.
}
