% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cache_object}
\alias{cache_object}
\title{Cache an object in an environment or cachem backend}
\usage{
cache_object(name, expressions, cache = parent.frame(), update = FALSE)
}
\arguments{
\item{name}{Character scalar. Cache key or object name.}

\item{expressions}{Expression to evaluate when the value is not already cached.
Use \code{{}} to group multiple statements.}

\item{cache}{Storage backend. Defaults to the caller environment.}

\item{update}{Logical scalar. If \code{TRUE}, recompute and overwrite any cached
value.}
}
\value{
The cached or newly computed value, returned invisibly.
}
\description{
Evaluate an expression and store its result under \code{name}. If a cached value is
already available and \code{update = FALSE}, that value is returned instead.
}
\details{
\code{cache} can be either:
\itemize{
\item an \code{\link[=environment]{environment()}}, where values are stored with \code{\link[base:assign]{base::assign()}}
\item a \code{cachem} cache object, where values are stored with \verb{$set()} / \verb{$get()}
}
}
\examples{
env <- new.env(parent = emptyenv())

value <- cache_object(
  "x",
  {
    message("computing")
    1 + 1
  },
  cache = env
)

value <- cache_object("x", stop("will not run"), cache = env)

value <- cache_object("x", 3, cache = env, update = TRUE)
}
