% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cache_result}
\alias{cache_result}
\title{Cache the value of an expression on disk}
\usage{
cache_result(expressions, filename, update = FALSE)
}
\arguments{
\item{expressions}{Expression to cache. Use \code{{}} to group multiple statements.
The expression is evaluated in a child environment of the caller.}

\item{filename}{Cache file name or path. If \code{filename} has no directory
component, or points to the current working directory, the cache is stored
under a local \verb{cache/} subdirectory.}

\item{update}{Logical scalar. If \code{TRUE}, always recompute the value and
overwrite any existing cache file.}
}
\value{
The evaluated result.
}
\description{
Evaluate an expression once and persist its result to a cache file.
Subsequent calls return the cached value unless \code{update = TRUE}.
}
\details{
File format is inferred from \code{filename}:
\itemize{
\item \code{.rds} uses \code{\link[base:readRDS]{base::readRDS()}} / \code{\link[base:saveRDS]{base::saveRDS()}}
\item any other extension is treated as a \code{qs} cache and uses \code{qs::qread()} /
\code{qs::qsave()}
}
}
\examples{
\dontrun{
# Cache random numbers to cache/my_random_numbers.qs
rand_nums <- cache_result(
  {
    message("Generating random numbers...")
    rnorm(10)
  },
  "my_random_numbers.qs"
)

# Reuse the cached value on later calls
rand_nums_again <- cache_result(rnorm(10), "my_random_numbers.qs")

# Force recomputation
rand_nums_new <- cache_result(rnorm(10), "my_random_numbers.qs", update = TRUE)

# Use RDS instead of qs
data_rds <- cache_result(data.frame(x = 1:5, y = letters[1:5]), "my_data.rds")
}
}
