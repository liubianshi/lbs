% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lake.R
\name{read_archive_lake}
\alias{read_archive_lake}
\title{Read a registered raw entity from the econ-data data lake.}
\usage{
read_archive_lake(
  database,
  table,
  var = NULL,
  condition = NULL,
  and = TRUE,
  limit = NULL,
  noinfo = TRUE,
  lake_path = NULL
)
}
\arguments{
\item{database}{lbs database name (e.g. \code{"CHN_FirmTrade"}).}

\item{table}{lbs table name (e.g. \code{"HG2005"}).}

\item{var}{optional character vector of column names to project.}

\item{condition}{optional SQL \code{WHERE} clause(s); same semantics as
\code{\link[=read_archive]{read_archive()}}.}

\item{and}{\code{TRUE} (default) joins \code{condition} with \code{AND}; \code{FALSE} with \code{OR}.}

\item{limit}{optional integer row cap.}

\item{noinfo}{if \code{TRUE} (default) return the \code{data.table} directly; if
\code{FALSE} return \code{list(data, info)} matching \code{\link[=read_archive]{read_archive()}}.}

\item{lake_path}{data lake root. Defaults to the env var
\code{ECON_DATA_LAKE_PATH}, falling back to \verb{~/Data/econ-data-lake}.}
}
\value{
\code{data.table} (or \code{list(data, info)} if \code{noinfo = FALSE}).
}
\description{
Compatibility shim during the migration from lbs's \verb{~/Data/DBMS} archive
to the econ-data data lake. Translates lbs \verb{(database, table)} coordinates
to the lake's \verb{raw:<source>/<dataset>@<version>} id format and reads the
corresponding \code{data.parquet}. Column labels stored in the entity's
\code{meta.yaml} are attached to the result via \code{\link[=stlabel]{stlabel()}}, matching the
attribute shape of \code{\link[=read_archive]{read_archive()}}.
}
\details{
Naming rules mirror the migration generator (\code{gen_mapping.R}):
\itemize{
\item \code{source}  = \code{tolower(database)} with a leading \code{"chn_"} stripped
\item \code{dataset} = \code{tolower(table)} minus a trailing 4-digit year suffix
(optionally \code{Q1}–\code{Q4})
\item \code{version} = the stripped suffix in lowercase, or \code{"v1"} if absent
}

Examples of the mapping:
\itemize{
\item \code{CHN_FirmTrade}, \code{HG2005}      → \verb{raw:firmtrade/hg@2005}
\item \code{CHN_FirmTrade}, \code{HG2005Q4}    → \verb{raw:firmtrade/hg@2005q4}
\item \code{Coding_Inds},   \code{HS92_ISIC2}  → \code{raw:coding_inds/hs92_isic2@v1}
}
}
\seealso{
\code{\link[=read_archive]{read_archive()}} for the legacy file-based reader.
}
