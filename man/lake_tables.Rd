% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lake-catalog.R
\name{lake_tables}
\alias{lake_tables}
\title{List every entity (table) in the data lake.}
\usage{
lake_tables(
  type = NULL,
  domain = NULL,
  status = NULL,
  use_registry = FALSE,
  lake_path = NULL
)
}
\arguments{
\item{type}{optional filter on entity type (\code{raw} / \code{dim} / \code{concord} /
\code{indicator}).}

\item{domain}{optional filter on domain (e.g. \code{"firmtrade"}).}

\item{status}{optional filter on status (\code{draft} / \code{stable} / \code{deprecated}).}

\item{use_registry}{if \code{TRUE}, read \code{registry/entities.parquet} instead of
scanning meta.yaml. Defaults to \code{FALSE} (always-current disk scan).}

\item{lake_path}{data lake root. See \code{\link[=lake_read]{lake_read()}}.}
}
\value{
A \code{data.table} with columns \code{id}, \code{type}, \code{domain}, \code{dataset},
\code{version}, \code{name}, \code{status}, \code{author}, \code{updated_at}, \code{has_data}.
}
\description{
By default scans the entity \code{meta.yaml} files on disk, so the result is
always current. Set \code{use_registry = TRUE} to instead read the pre-built
\code{registry/entities.parquet} index — faster on a large lake, but only as
fresh as the last \verb{econ-data lake index} run. The \code{dataset} column is
derived from the entity id and is the natural join key into
\code{\link[=lake_variables]{lake_variables()}} / \code{\link[=lake_columns]{lake_columns()}}.
}
\seealso{
\code{\link[=list_tables]{list_tables()}} for the legacy archive index.
}
