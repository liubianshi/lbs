% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cache_fn}
\alias{cache_fn}
\title{Wrap a Function with Disk-Backed Caching and Expiration}
\usage{
cache_fn(f, subdir = NULL, expire_days = 7L)
}
\arguments{
\item{f}{Function to wrap.}

\item{subdir}{Optional cache subdirectory name. If \code{NULL}, a directory name is
inferred from the function environment.}

\item{expire_days}{Positive numeric scalar. Cache entries older than this many
days are considered expired.}
}
\value{
A function with the same arguments as \code{f}, plus a cache-control
argument. By default that argument is named \code{update}; if \code{f} already has an
argument named \code{update}, the added argument is renamed to \code{.update}.
}
\description{
Create a cached variant of \code{f} that stores results on disk, reuses previous
results for identical calls, and can refresh stale cache entries after a
configurable number of days.
}
\details{
Cache files are stored below the XDG cache directory:
\itemize{
\item \code{file.path(Sys.getenv("XDG_CACHE_HOME"), "R", "function", subdir)} when
\code{XDG_CACHE_HOME} is set
\item otherwise \code{file.path(Sys.getenv("HOME"), ".cache", "R", "function", subdir)}
}

In interactive sessions, expired cache entries trigger a prompt asking whether
to refresh the cached value. In non-interactive sessions, stale cache is used
unless the cache-control argument is explicitly set to \code{TRUE}.

Results are stored together with a timestamp so expiration can be checked on
later calls.
}
\examples{
\dontrun{
slow_fetch <- function(id) {
  Sys.sleep(2)
  data.frame(id = id, value = rnorm(10))
}

fast_fetch <- add_cache(slow_fetch, subdir = "myapp", expire_days = 14)

# First call computes and caches
res1 <- fast_fetch(123)

# Second call reuses the cache
res2 <- fast_fetch(123)

# Force refresh
res3 <- fast_fetch(123, update = TRUE)
}
}
